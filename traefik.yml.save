log:
  level: DEBUG

providers:
  docker:
    exposedbydefault: false
    network: proxy

entrypoints:
  http:
    address: ":80"
  https:
    address: ":443"

middlewares:
  redirect-to-https:
    redirect_scheme:
      scheme: https
